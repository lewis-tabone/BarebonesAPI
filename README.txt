
Run in dev mode > dotnet run

Build for production > dotnet publish

Launch production build > dotnet .../BarebonesAPI.dll

Controller can be accessed at this location > https://localhost:5001/WeatherForecast

Swagger UI can be access here. > https://localhost:5001/swagger/index.html (Currently configured to work in production)

